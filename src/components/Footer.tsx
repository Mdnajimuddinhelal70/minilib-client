const Footer = () => {
  return (
    <div>
      <footer className="bg-gray-100 text-center text-sm text-gray-500 py-3 mt-4">
        © {new Date().getFullYear()} MiniLib. Built with by me.
      </footer>
    </div>
  );
};

export default Footer;

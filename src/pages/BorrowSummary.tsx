import BorrowSummaryItems from "../components/borrows/BorrowSummaryItems";

const BorrowSummary = () => {
  return (
    <div>
      <BorrowSummaryItems />
    </div>
  );
};

export default BorrowSummary;
